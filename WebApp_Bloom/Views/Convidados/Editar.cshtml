<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery.mask/1.14.11/jquery.mask.min.js"></script>


<h2>Editar Convidado</h2>
<div class="cardprincipal">
    <form>
        <div class="row">
            <div class="col">
                <div class="form-group-right">
                    <label>Nome:</label>
                    <input type="text" value="" required pattern="[A-Za-záàâãéèêíïóôõöúçñÁÀÂÃÉÈÍÏÓÔÕÖÚÇÑ ]+$" onkeypress="return ApenasLetras(event,this);" />
                </div>
                <div class="form-group-right">
                    <label>Mesa:</label>
                    <input type="number" value="" required/>
                </div>
                <div class="form-group-right">
                    <label>Telefone:</label>
                    <input type="tel" id="telefone2" name="telefone2" value="" pattern="\([0-9]{2}\)[\s][0-9]{4}-[0-9]{4,5}" />
                </div>
            </div>
            <div class="col form-group-center">
                <div class="botoes">
                    <div class="form-group-center">
                        <button action="/Convidados/SalvarDados" class="botao" type="submit">Salvar</button>
                    </div>
                </div>
            </div>
        </div>
    </form>
</div>

<script>
    $(document).ready(function () {
        $('#telefone').mask('(00) 00000-0000');
        $('#telefone1').mask('(00) 00000-0000');
        $('#telefone2').mask('(00) 00000-0000');
        $('#telefone3').mask('(00) 00000-0000');
        $('#rg').mask('00.000.000-0');
        $('#rg1').mask('00.000.000-0');
    });

    function ApenasLetras(e, t) {
        try {
            if (window.event) {
                var charCode = window.event.keyCode;
            } else if (e) {
                var charCode = e.which;
            } else {
                return true;
            }
            if (
                (charCode > 64 && charCode < 91) ||
                (charCode > 96 && charCode < 123) ||
                (charCode > 191 && charCode <= 255) // letras com acentos
            ) {
                return true;
            } else {
                return false;
            }
        } catch (err) {
            alert(err.Description);
        }
    }
</script>